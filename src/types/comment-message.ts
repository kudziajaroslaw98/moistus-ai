/**
 * Comment Message Types
 *
 * Re-export from comment.ts for convenience and backwards compatibility
 */

export type {
	CommentMessage,
	CommentReaction,
	GroupedReaction,
	CreateMessageRequest,
	CreateReactionRequest,
} from './comment';
